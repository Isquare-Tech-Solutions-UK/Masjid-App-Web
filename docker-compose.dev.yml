services:
  app:
    image: ${DOCKER_USERNAME}/masjid-app-frontend:dev-latest
    container_name: masjid-frontend-dev
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - BACKEND_API_URL=http://72.62.6.65:8080